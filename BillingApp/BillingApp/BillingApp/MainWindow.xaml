diff --git a//dev/null b/BillingApp/MainWindow.xaml
index 0000000000000000000000000000000000000000..ddb27372cff7c2ae2576d7630bfbb7b31c55b314 100644
--- a//dev/null
+++ b/BillingApp/MainWindow.xaml
@@ -0,0 +1,82 @@
+<Window x:Class="BillingApp.MainWindow"
+        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
+        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
+        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
+        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
+        xmlns:vm="clr-namespace:BillingApp.ViewModels"
+        mc:Ignorable="d"
+        Title="Billing" Height="700" Width="900">
+    <Window.DataContext>
+        <vm:MainViewModel />
+    </Window.DataContext>
+    <DockPanel>
+        <StatusBar DockPanel.Dock="Bottom">
+            <StatusBarItem>
+                <TextBlock Text="{Binding CurrentDateTime, StringFormat='{}{0:F}'}" />
+            </StatusBarItem>
+            <StatusBarItem HorizontalAlignment="Right">
+                <TextBlock Text="SQL-backed Billing" />
+            </StatusBarItem>
+        </StatusBar>
+        <ScrollViewer VerticalScrollBarVisibility="Auto">
+            <StackPanel Margin="16" Spacing="16">
+                <StackPanel>
+                    <TextBlock Text="Customer Name" FontSize="14" FontWeight="Bold" />
+                    <TextBox Text="{Binding CustomerName, UpdateSourceTrigger=PropertyChanged}" Height="28" />
+                </StackPanel>
+                <StackPanel>
+                    <TextBlock Text="Instructions" FontSize="14" FontWeight="Bold" />
+                    <TextBox Text="{Binding Instructions, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" AcceptsReturn="True" />
+                </StackPanel>
+
+                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
+                    <Button Content="Add Item" Command="{Binding AddItemCommand}" Width="100" />
+                    <Button Content="Remove Item" Command="{Binding RemoveItemCommand}" Width="120" />
+                    <Button Content="Manage Products" Width="150" Click="OnManageProducts" />
+                </StackPanel>
+
+                <DataGrid ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" AutoGenerateColumns="False" CanUserAddRows="False" HeadersVisibility="Column" IsReadOnly="False" Height="400">
+                    <DataGrid.Columns>
+                        <DataGridTextColumn Header="S/N" Binding="{Binding SerialNumber}" IsReadOnly="True" Width="60" />
+                        <DataGridTemplateColumn Header="Product" Width="200">
+                            <DataGridTemplateColumn.CellTemplate>
+                                <DataTemplate>
+                                    <TextBlock Text="{Binding ProductName}" />
+                                </DataTemplate>
+                            </DataGridTemplateColumn.CellTemplate>
+                            <DataGridTemplateColumn.CellEditingTemplate>
+                                <DataTemplate>
+                                    <ComboBox ItemsSource="{Binding DataContext.Products, RelativeSource={RelativeSource AncestorType=Window}}"
+                                              DisplayMemberPath="Name"
+                                              SelectedValuePath="Name"
+                                              SelectedValue="{Binding ProductName, UpdateSourceTrigger=PropertyChanged}"
+                                              IsEditable="True" />
+                                </DataTemplate>
+                            </DataGridTemplateColumn.CellEditingTemplate>
+                        </DataGridTemplateColumn>
+                        <DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*" />
+                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="100" />
+                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}" Width="120" />
+                        <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=C2}" IsReadOnly="True" Width="120" />
+                    </DataGrid.Columns>
+                </DataGrid>
+
+                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="24">
+                    <StackPanel>
+                        <TextBlock Text="Total Quantity" FontWeight="Bold" />
+                        <TextBlock Text="{Binding TotalQuantity}" FontSize="16" />
+                    </StackPanel>
+                    <StackPanel>
+                        <TextBlock Text="Total Amount" FontWeight="Bold" />
+                        <TextBlock Text="{Binding TotalAmount, StringFormat=C2}" FontSize="16" />
+                    </StackPanel>
+                </StackPanel>
+
+                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
+                    <Button Content="Save Invoice" Width="120" Click="OnSaveInvoice" />
+                    <Button Content="Print A5" Width="120" Click="OnPrint" />
+                </StackPanel>
+            </StackPanel>
+        </ScrollViewer>
+    </DockPanel>
+</Window>
