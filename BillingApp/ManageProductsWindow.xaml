diff --git a//dev/null b/BillingApp/ManageProductsWindow.xaml
index 0000000000000000000000000000000000000000..212a91e55799d0ac3687eba06b3876f8b67450ce 100644
--- a//dev/null
+++ b/BillingApp/ManageProductsWindow.xaml
@@ -0,0 +1,49 @@
+<Window x:Class="BillingApp.ManageProductsWindow"
+        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
+        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
+        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
+        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
+        mc:Ignorable="d"
+        Title="Product Catalog" Height="400" Width="600">
+    <Grid Margin="12">
+        <Grid.RowDefinitions>
+            <RowDefinition Height="Auto" />
+            <RowDefinition Height="*" />
+            <RowDefinition Height="Auto" />
+        </Grid.RowDefinitions>
+
+        <Grid>
+            <Grid.ColumnDefinitions>
+                <ColumnDefinition Width="220" />
+                <ColumnDefinition Width="150" />
+                <ColumnDefinition Width="*" />
+            </Grid.ColumnDefinitions>
+            <StackPanel Grid.Column="0">
+                <TextBlock Text="Name" FontWeight="Bold" />
+                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
+            </StackPanel>
+            <StackPanel Grid.Column="1" Margin="8,0">
+                <TextBlock Text="Unit Price" FontWeight="Bold" />
+                <TextBox Text="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" />
+            </StackPanel>
+            <StackPanel Grid.Column="2">
+                <TextBlock Text="Description" FontWeight="Bold" />
+                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />
+            </StackPanel>
+        </Grid>
+
+        <DataGrid Grid.Row="1" Margin="0,12,0,12" ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}" AutoGenerateColumns="False">
+            <DataGrid.Columns>
+                <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*" />
+                <DataGridTextColumn Binding="{Binding UnitPrice}" Header="Price" Width="100" />
+                <DataGridTextColumn Binding="{Binding Description}" Header="Description" Width="2*" />
+            </DataGrid.Columns>
+        </DataGrid>
+
+        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
+            <Button Content="Save" Command="{Binding SaveCommand}" Width="100" />
+            <Button Content="Delete" Command="{Binding DeleteCommand}" Width="100" />
+            <Button Content="Close" Width="100" Click="OnCloseClicked" />
+        </StackPanel>
+    </Grid>
+</Window>
